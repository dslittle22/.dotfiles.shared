[color]
	ui = auto
[core]
	editor = nvim
	pager = less -r --mouse
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[push]
	autoSetupRemote = true
[remote "origin"]
	prune = true
